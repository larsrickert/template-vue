import type { NumberFormats } from "@intlify/core-base";

const numberFormats = {
  en: {
    currency: {
      style: "currency",
      currency: "USD",
    },
  },
} satisfies NumberFormats;

export default numberFormats;
